<?php

use Illuminate\Support\Facades\Route;
use Modules\{{ module }}\Http\Controllers\{{ module }}Controller;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
*/

Route::prefix('v1/{{ module_kebab }}')->middleware('api')->group(function () {
    Route::apiResource('/', {{ module }}Controller::class);
});
